import 'package:flutter/material.dart';

class CULoading extends StatelessWidget {
  const CULoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
